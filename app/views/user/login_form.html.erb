<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
 <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>login</title>
</head>
<body>
    <div class="page_title">Login</div>
    <br>
      <% if @error_message %>
        <div class="form-error">
          <%= @error_message %>
        </div>
      <% end %>
    <br>
      <%= form_with url:"/users/login" ,class:"input_form" do |f|%>
        <table class="form_table">
          <tr>
            <td>Login ID</td>
          </tr>
          <tr>
            <td><%= f.text_field :login_id ,:value => @login_id %></td>
          </tr>
          <tr>
            <td>Password</td>
          </tr>
          <tr>
            <td><%= f.password_field :password ,:value => @password %></td>
          </tr>
          <tr>
            <td><%= f.submit 'Login',:class=>"form_btn" %></td>
          </tr>
        </table>
      <% end %>
    </div>
</body>
</html>