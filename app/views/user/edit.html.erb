<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
 <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>アカウント設定編集</title>
</head>
<body>
    <div class="page_title">アカウント編集</div>
    <br>    
        <% @current_user.errors.full_messages.each do |message| %>
            <div class="form-error">
                <%= message %>
            </div>
        <% end %>
    <br>
        <%= form_with url:"/users/update",class:"edit_form" do |f|%>
            <p>Login ID</p>
            <%= f.text_field :login_id ,:value => @current_user.login_id %>
            <p>Password</p>
            <%= f.password_field :password %>
            <br>
            <%= f.submit '登録'%> 
        <% end %>
    <br>
    <div class="menulist">
        <li><%= link_to("戻る","/users/#{@current_user.id}")%></li>
    </div>
</body>
</html>