<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>アカウント設定編集</title>
</head>
<body>
    <div class="input_table">
        <div class="input_table_title"><i class="fas fa-user-edit"></i>アカウント編集<%= link_to "/users/#{@current_user.id}" ,class:"close_btn" do %><i class="fa-regular fa-circle-xmark"></i><%end%></div> 
            <div class="form-error">
                <% @current_user.errors.full_messages.each do |message| %>
                    <%= message %>
                <% end %>
            </div>
            <div class="account_edit_main">
                <%= form_with url:"/users/update" do |f|%>
                    <table class="account_form">
                        <tr>
                            <td>
                                <div class="label">ID</div>
                            </td>
                            <td>
                                <%= f.text_field :login_id ,value:@current_user.login_id , class:"input"%>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                <div class="label">Password</div>
                            </td>
                            <td>
                                <%= f.password_field :password, class:"password_input"%>
                            </td>
                        </tr>
                    </table>
                    <%= f.submit '変更', class:"user_form_btn"%>
                <% end %>
            </div>
    </div>
</body>
</html>