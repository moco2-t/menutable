<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
 <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Account</title>
</head>
<body>
    <div class="page_title">Account</div>
    <br>
        <%if @user %>
            <% @user.errors.full_messages.each do |message| %>
                <div class="form-error">
                    <%= message %>
                </div>
            <% end %>
        <%end%>
    <br>
        <%= form_with url:"/users/create",class:"input_form" do |f|%>
            <table class="form_table">
                <tr>
                    <td>Login ID</td>
                </tr>
                <tr>
                    <td><%= f.text_field :login_id ,:value => @login_id %></td>
                </tr>
                <tr>
                    <td>Password</td>
                </tr>
                <tr>
                    <td><%= f.password_field :password ,:value => @password %></td>
                </tr>
                <tr>
                    <td><%= f.submit '登録',:class=>"form_btn" %></td>
                </tr>
            </table>
        <% end %>
</body>
</html>