<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
 <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Shopping</title>
</head>
<body>
    <div class="input_table_2">
        <div class="input_table_title">New shoppng list<%= link_to shopping_index_path,class:"close_btn" do %><i class="fa-regular fa-circle-xmark"></i><%end%></div>
        <div class='central_part'>
            <div class="description">日付を選択してください。</div>
            <div class="form_shopping_list">
                <%= form_with url:"/shopping/sum_shopping_list" do |f| %>
                    <%= f.date_field :start_date ,class:"shopping_select_date"%> 〜 <%= f.date_field :start_date2,class:"shopping_select_date"%>
                    <%= f.button :type => "submit",class:"create_shoppinglist_btn" do %><i class="fas fa-folder-plus"></i><% end %>
                <% end %>
            </div>
            <div class="menu_calendar_shopping_list_creation">
                <%= week_calendar number_of_weeks:2,events:@menus do |date,menus| %>
                    <%= date.day %>
                    <% menus.each do |m| %>
                        <div>
                            <li><%= link_to Recipe.find_by(id:m.recipe_id).name,Recipe.find_by(id:m.recipe_id).address,class:"recipe_link" %></li>
                        </div>
                    <% end %>
                <% end %>
            </div>
        </div>
    </div>
</body>
</html>
