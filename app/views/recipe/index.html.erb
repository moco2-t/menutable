<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
 <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Recipe index</title>
</head>
<body>
    <div class="page_title"><h1>Recipe</h1></div>
      <div class="menu">
        <li><%= link_to("URLで登録","/recipes/new")%></li>
        <li><%= link_to("自分で入力して登録","")%></li>
      </div>
      <br>
      <br>
        <%= form_with url:"/recipes/search", class:"search_form", method: :get, local: true do |f| %>
          <%= f.label :search,'レシピ検索'%>
          <%= f.text_field :searchword %>
          <%= f.submit "search",name: nil%>
          <%= link_to "戻る","/recipes/index" %>
        <% end %>
      <br>
        <%if @search_recipe%>
          <table class="search_recipeslist">
            <% @search_recipe.each do |s| %>
              <tr>
                <td>
                  <%= link_to s.name,  "/recipes/#{s.id}" %>
                </td>
              </tr>
            <%end%>
          </table>
        <%else%>
          <table class="all_recipeslist">
            <% recipes().each do |r|%>
              <tr>
                <td>
                  <%= link_to r.name ,  "/recipes/#{r.id}" %>
                </td>
              </tr>
            <%end%>
          </table>
        <%end%>
        <br>

</body>
</html>